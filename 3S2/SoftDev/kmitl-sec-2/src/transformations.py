import pandas as pd
def normalize_columns(df: pd.DataFrame) -> pd.DataFrame:
    """Converts columns to lowercase and replaces spaces with underscores."""
    pass
def replace_percent(df: pd.DataFrame) -> pd.DataFrame:
    """Strip % convert to float and devide by 100"""
    pass