# Development-only example values. Never commit a real .env file.
POSTGRES_USER=admin
POSTGRES_PASSWORD=dev-only-password
POSTGRES_DB=mydb
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=dev-only-password
DB_NAME=mydb
DB_SSLMODE=disable
JWT_SECRET=dev-only-jwt-secret
